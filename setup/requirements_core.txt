# =============================================================
# Core Dependencies — REQUIRED for ChangeGUI to run
# =============================================================
# Run: pip install -r requirements_core.txt

Pillow>=10.0.0
numpy>=1.24.0
requests>=2.28.0
opencv-python>=4.8.0
moviepy>=1.0.3
soundfile>=0.12.0
pydub>=0.25.0
openpyxl>=3.1.0
pandas>=2.0.0

# Edge TTS (online Microsoft TTS — lightweight, no extra model downloads)
edge-tts>=6.0.0
